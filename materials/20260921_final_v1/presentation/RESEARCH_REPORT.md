# ScamLens 최종 연구 보고서 (2026-09-21)

## 연구 질문
정상 문자 경보(오탐)를 늘리지 않으면서 스미싱 미탐을 줄이려면 무엇을 검증해야 하는가.

## 평가 설계
- 기존 U5 시험자료 2,317건(정상 1,665건·스미싱 652건,
  과거 평가 이력 있음), 시드 42, 101, 202, 303, 404.
- KISA 외부 양성 22건(대표 18건은 부분집합),
  정상 후보 250건(개발용, 독립 오탐률 아님).
- JEV 자동 대체 선택 88건(오류 과대표집), JEV 특징 결합 142건 탐색.
- 임계값은 평가 전에 고정하고 모든 입력 조건에 동일하게 적용. 오프라인 문자열 분석만 사용.

## 조건 설계
- BASE 원본 학습, DUP 선택 원문 추가, FLIP 선택 문자 URL 편집문 추가.
  FLIP은 정답 라벨을 유지하며 라벨을 반전하지 않는다.

## 사전 SAS 분석과 U5 구분
- SAS 서버 OOF 메타 분석: 문자 단독 AUC 0.9987,
  SAS OOF 메타 0.9988,
  고정가중 결합 0.9950.
  차이 추정 -0.00006,
  95% CI [-0.00039, 0.00027],
  p=0.7233. 차이가 검출되지 않았으며 동등성 증명이 아니다.
- SAS 09-08 분석은 SAS 서버 환경에서 OOF를 메타 모형 학습 입력으로 사용하고 테스트 분할(where split='test')에서 ROC를 비교한 결과임. 최신 U5 실험은 로컬 Python/MPS 환경에서 수행된 KcBERT 대조실험임. 두 실행 환경과 모형이 상이하므로 SAS 분석의 p값을 U5 결과에 적용하지 않음.

## U5 결과
BASE 평균 오탐 10.0건·미탐 14.2건, DUP 평균 오탐 7.4건·미탐 13.8건, FLIP 평균 오탐 7.6건·미탐 16.0건. DUP 평균이 가장 낮았으나 통계적 우월 확정 아님.
- URL 정상: 있음 집단 평균 오탐률 2.01%
  (정상 298건), 없음 집단 0.10%
  (정상 1367건). 연관 관찰이며 단일 원인 확정 아님.
- 길이: 128토큰 초과 스미싱 57건의
  미탐률 16.84%.
  한도 이내 595건 미탐률
  0.71%.

## KISA 외부 진단(기준 시드 42)
- 고유 22건 중 13건 탐지
  9건 미탐, 대표 18건 중
  9건 탐지 9건 미탐.
  KISA 양성만 있어 오탐률 계산 불가.

| 입력 처리 | 탐지/미탐 | 탐지율 | 정상 후보 경보 | 새 경보 |
|---|---:|---:|---:|---:|
| original128 | 13/9 | 59.09% | 11건 | 0건 |
| ocr_delete128 | 13/9 | 59.09% | 11건 | 0건 |
| cap300 | 13/9 | 59.09% | 24건 | 13건 |
| ocr_delete300 | 14/8 | 63.64% | 24건 | 13건 |
| ocr_unk128 | 13/9 | 59.09% | 11건 | 0건 |
| ocr_unk300 | 16/6 | 72.73% | 24건 | 13건 |
| head_tail128 | 13/9 | 59.09% | 17건 | 6건 |

- 사전 후보 `head_tail128`은 5개 시드 모두 미탐 복구 없이
  새 경보 발생으로 불통과. 관측 후 승자 교체를 하지 않고 기존 조건과 고정 임계값 유지.

## JEV 두 실험 분리
- 자동 대체(선택 88건): 기존 오탐 8건 해소,
  새 미탐 41건, 정답→보류 1건. 미채택.
- 특징 결합(142건): 기준선부터 오류 없어 추가 이득 입증 여지 없음.

| 결합 조건 | 오탐 | 미탐 |
|---|---:|---:|
| KcBERT 기준선 | 0건 | 0건 |
| KcBERT 점수만 재결합 | 0건 | 0건 |
| KcBERT＋JEV | 0건 | 0건 |
| KcBERT＋URL 특징 | 1건 | 0건 |
| KcBERT＋JEV＋URL 특징 | 0건 | 0건 |

## SAS 실행 검증
- 집계 반환 4단계 완료, ZIP 55개 파일 무결성,
  그래프 35개 검수.
  SAS 계산·반환 검증 통과와 탐지 성능 개선은 다른 판단이다.
- 새 SAS Studio 발표용 그림과 Visual Analytics 대시보드는 사용자 서버 실행 대기 중이며,
  실제 가동을 주장하지 않는다.

## 결론과 한계
- 오류 집중 조건과 시도한 수정의 득실, 채택하지 않을 방법을 가려낸 검증 과정이 성과다.
  원인 단일 확정·실서비스 안전성 입증이 아니다.
- 연구 완료는 실서비스 효능이 아니다. 새 독립 자료에서 사전 고정한 비교가 다음 단계다.

## 재생성
본 문서 수치는 reports/generated/submission_final_20260921.json에서 계산한다.
`python3 scripts/build_submission_final_20260921.py --check`로 정합성을 확인한다.
새 추론·API·학습·임계값 조정이 없다.
