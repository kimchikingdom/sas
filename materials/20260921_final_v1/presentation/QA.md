# ScamLens 질의응답 (2026-09-21)

## DUP가 가장 좋은가
- U5 평균(BASE 평균 오탐 10.0건·미탐 14.2건, DUP 평균 오탐 7.4건·미탐 13.8건, FLIP 평균 오탐 7.6건·미탐 16.0건)에서 DUP가 가장 낮았다. 통계적 우월 확정은 아니다.

## FLIP은 라벨을 뒤집는가
- 아니다. FLIP은 정답 라벨을 유지한 URL 편집문 추가 조건이다.

## 외부에서도 통하는가
- KISA 양성 22건 기준 탐지 59.09%
  는 소표본 결과이며 전체 환경 탐지율로 일반화하지 않는다.

## 입력을 고치면 해결되는가
- 7조건 탐색에서 안정적 개선이 없었다. 잡음 가림＋300토큰 일부 복구는 탐색적 관찰이며
  정상 경보도 함께 늘었다. 사후 승자 교체는 하지 않는다.

## JEV를 쓰지 않는 이유
- 자동 대체는 해소 8건 대비 신규 미탐 41건.
  특징 결합은 기준선 무오류라 이득 입증 여지가 없었다.

## SAS는 무엇을 검증했나
- 기존 집계 반환 4단계·ZIP 55개·그래프
  35개 대조. 성능 개선과 다른 판단이다.
  새 Studio 그림·VA 대시보드는 사용자 서버 실행 대기 중이다.

## 재현 방법
- `python3 scripts/build_submission_final_20260921.py --check`
- 집계 정본 reports/generated/submission_final_20260921.json에서 모든 수치를 다시 계산한다.
