# SAS 실행 체크리스트

현재 완료 범위와 결과 수치는 [SAS 현황](SAS_STATUS_20260914.md)을 따른다.
01·02·03·05·06·07의 기존 로그·HTML과 08의 반환 CSV·회수 출력표/로그는 확인했다.
새 실행기 정적 검사와 실제 SAS 실행은 구분한다.

## 이번 실행: 과거 A 단독 자료 재현

- [ ] 새 개인 이전 번들의 MANIFEST.json·CHECKSUMS_SHA256.txt 보관
- [ ] 내부 sas/data/reports/docs/outputs 폴더를 /home/student/에 함께 배치
- [ ] UTF-8 세션 및 쓰기 권한 확인
- [ ] sas/00_RUN_PENDING.sas 실행; 기존 출력 폴더가 있으면 run_tag를 새로 지정
- [ ] 09: A 단독 본문 판단·원 라벨·유사 정상 짝 교차표 확인
- [ ] 10: 시드별 development_readout에서 A 판단과 저장 선형 판정 교차표 확인
- [ ] 09·10 로그·HTML·pending_run_status.csv를 사용한 manifest와 함께 보관
- [ ] 실제 ERROR/WARNING·표 누락·입력 버전·교차표 수치를 검수한 뒤 완료 표시

09·10은 최신 A/B 합의나 삭제 개입 결과를 SAS로 분석하는 단계가 아니다.
원 라벨을 바꾸거나 A 판단을 정확도의 정답으로 사용하지 않는다.

## 선택 재현

- 전체 과거 분석: 00_RUN_ALL.sas (이전 출력은 먼저 보존)
- 08 재현: 동일 입력·시드별 적합·고정 보정 규칙 유지, 로그·HTML과 반환 CSV 함께 보관
- 04: CAS 세션·라이선스·WORK 공간을 확인한 경우에만 선택

계수나 p값이 예상과 다르다는 이유로 모델·임계값·평가 행을 바꾸지 않는다.
오류 대응과 단계별 검수 기준은 [런북](../sas/RUNBOOK.md)을 따른다.
