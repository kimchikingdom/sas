# SAS 실행 런북

실제 실행 여부는 사용자 SAS 세션의 로그·ODS 결과로 확인한다.
교육 최종 발표의 중심은 유형·유사도별 실패 조건이다.

## 최신 후속 실행 — 2026-09-21

이번 요청은 [SAS 후속 실행 안내](../docs/SAS_FOLLOWUP_RUN_GUIDE_20260921.md)의
두 줄을 새 SAS 세션에서 실행한다. 최신 Git main의 followup_20260921_v1 폴더
전체가 서버에 있어야 한다. 원문·개별 점수·모델 파일의 별도 업로드는 필요 없다.
후속 패키지 내부 manifest.json을 사용하며 루트의 과거 manifest와 구분한다.

이하 내용은 기존 A/B·과거 분석 재현용이다.

## 준비

1. 최신 번들 내부 `sas/`, `data/`, `reports/`, `outputs/`를 `/home/student/github/`에 배치한다.
2. `CHECKSUMS_SHA256.txt`와 `MANIFEST.json`을 보관한다. 이전 CSV와 새 코드를 섞지 않는다.
3. `proc options option=encoding value; run;`으로 UTF-8을 확인한다.
4. `outputs/` 쓰기 권한을 확인한다. 04를 실행할 때만 추가 WORK 공간·라이선스를 확인한다.

## 기존 실행: A/B 검수 후 분석

`/home/student/github/sas/00_RUN_AB.sas`를 전체 실행한다. 순서는 11 → 12다.
09·10은 `archive/`로 이동했으며 `00_RUN_PENDING.sas`도 실행 안내만 남긴다.
매 실행은 새 `outputs/ab_20260914_실행별코드/` 폴더를 만들며 기존 결과는 보존한다.

11은 합의 전 A/B 원응답의 일치도, 12는 최종 합의 범주별 저장 모델 판정이다.
원 라벨 기준 오류와 본문 판단을 구분하고, 학습·임계값 조정을 수행하지 않는다.

## 전체 과거 분석 재현

`00_RUN_ALL.sas`는 07 → 01 → 02 → 03 → 06 → 05 순서로 시작하며 04는 선택 실행이다.
08은 `sas_fage_abc_rows.csv`가 있으면 실행한다. 09·10은 실행하지 않는다.
현재 확인된 과거 결과를 다시 얻기 위해 전체 실행할 필요는 없다.
08은 원 train fold 0 개발 자료의 시드별 적합이며 01의 평가 범위와 다르다.

02는 유효한 OOF 입력이 있을 때 적합한다. OOF 부재로 건너뛴 결과를 완료로 부르지 않는다.
단독 실행 시 07·08은 독립 실행할 수 있고, 내부 분석은 01부터 시작한다.
04는 활성 CAS 세션 `mysess`와 TextMine/HPL 관련 이용 환경 확인이 필요한 선택 실험이다.

## 성공 판정

| 단계 | 확인 내용 |
|---|---|
| 공통 | 입력 버전, UTF-8, ERROR, WARNING 원인 |
| 07 | 전체→연도 적격→주요 유형 표본 흐름, 제외 미탐 수, 동일 범주 처리 |
| 07 | 수렴·분리, 동일 관측 수, joint year 검정, OR CI·관측/예측 곡선 |
| 01 | 실제 사용 바이트 기준 잘림, ID·분할·라벨·확률 범위 |
| 02 | train OOF 유효성, validation 임계값·적합 경고 |
| 03/06 | 같은 행의 ROC-AUC 비교, 결측에 따른 제외 행 |
| 05 | 입력 산출물에 연결된 그림, 모델·표본·전처리 단계 |
| 08 | 시드별 임계값·예측 CSV, 실제 수렴 로그·오류·보정/결과 확인 구간 분리 |
| 11 | 합의 전 A/B 원응답 범주·행동 일치도와 kappa |
| 12 | 같은 시드·ID의 원 라벨 기준 오류와 최종 합의 범주별 비교 |
| 13 | 선택 실행 시 CAS 적재 전 11·12 완료 문맥과 덮어쓰기 방지 확인 |

07은 Python과 같은 추정 설정이면 수치 대조가 가능하다. 차이가 나면 데이터형·기준범주·
제외 행·수렴·버전을 확인한다. 02는 Python과 규제·가중치가 달라 계수 부호를 합격
기준으로 쓰지 않는다. 어떤 p값도 미리 정한 합격 숫자가 아니다.

## 보관과 오류 대응

`outputs/`의 단계별 로그·HTML·실행 상태 CSV를 입력 manifest와 함께 내려받는다.
다음 실행 전 이전 결과를 날짜별 폴더로 보관한다. ERROR/WARNING 및 누락 단계를
검수한 뒤 완료 여부를 결정한다. 파일 존재만으로 성공이라고 표시하지 않는다.

오류가 나면 첫 ERROR, SAS 버전, 입력 해시를 기록한다. OOF 검사가 실패하면 입력을
대조하며 in-sample 점수로 우회하지 않는다. `%abort cancel`이 실행을 중단한 경우
최종 실행 상태 CSV가 없을 수 있으며, 완료로 간주하지 않는다.

발표에는 데이터 감사, 실패 확률·CI, 표본 제외 영향, ROC-AUC 차이를 골라 쓴다.
KISA 악성·URL 포함 자료와 선별 정상 도전셋은 현장 대표 표본이 아니다. 현장 유병률을
모르면 조건부 시나리오로 표시한다. 수신 문자 총수만으로는 유병률을 구할 수 없다.
